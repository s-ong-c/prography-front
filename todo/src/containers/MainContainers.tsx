import * as React from 'react';
import styled from 'styled-components';

const MainContainersBlock = styled.div``;
interface MainContainersProps{}

const MainContainers: React.SFC<MainContainersProps> = props => {
  return <MainContainersBlock />;
  };

export default MainContainers;